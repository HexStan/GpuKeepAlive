# GpuKeepAlive

GPU 保活工具：向目标显卡持续提交微量 D3D 渲染负载，维持显卡活跃就绪。含 GUI 托盘版（`GpuKeepAlive.exe`）与 CLI 命令行版（`GpuKeepAliveCli.exe`），.NET 10，仅支持 Windows x64。

---

# 代码规范性要求

- 如果现有代码结构无法规范、优雅地实现维护者的需求：

  - 大胆增/删/改写模块，以保持低耦合、高内聚，避免出现补丁式代码。
  - 允许引入新库，不要为了“不增加依赖”而频繁自己造轮子。
  
- 如果：

  - 不确定某个功能应该使用何种方案来实现
  - 应该如何处理某种维护者未提及的情况
  - 维护者的需求描述或功能设计中有无法自洽的内容，或 Agent 认为有更好的方案
  - 有其他任何不确定的问题

  请向维护者询问。可在开发过程中多次询问，直至所有细节清晰。

---

# 版本发布行为约束

- **版本号由维护者自行管理**：维护者负责决定进位并修改 `VERSION` 文件（不提交）；Agent 不得决定版本号进位，不得主动修改 `VERSION` 内容
- **发布触发条件**：仅在维护者主动下达发布版本的指令时，才进入版本号检查及发布流程，除此之外的任何情况下都不执行发布
- 发布操作的完整流程（前置条件、脚本用法、手动发布、撤回）详见 [RELEASE-GUIDE.md](RELEASE-GUIDE.md)
